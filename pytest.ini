[pytest]
addopts = -m "not gateway"
testpaths = tests
pythonpath = src
markers =
    live: integration test that connects to a real backend session (TTS or CTP)
    live_trade_window: integration test that submits real orders and requires an active trading session
    gateway: tests for the gateway module (market data, trading, integration)
