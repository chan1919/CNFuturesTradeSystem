[pytest]
addopts = -m "not gateway"
markers =
    live: integration test that connects to a real CTP server
    live_trade_window: live test that requires active trading session
    gateway: tests for the CTP gateway module (market data, trading, live order)